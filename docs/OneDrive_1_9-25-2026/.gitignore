# SME source documents (PDFs/DOCXs) are >60 MB total — not versioned in git.
# Regenerate by re-extracting the OneDrive_1_9-25-2026.zip into this folder.
# See README.md for the mapping between these source files and prototype fixtures.
*.pdf
*.docx
